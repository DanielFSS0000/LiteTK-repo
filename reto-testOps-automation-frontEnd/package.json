{
  "name": "reto-testops-automation-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Reto de automatizacion front-end Lite Bank con Playwright y POM.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
